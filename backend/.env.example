# Backend environment example for ConvertFlix

# Server
NODE_ENV=development
PORT=5000
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Admin seeding (ensures you can log in to admin panel)
INIT_ADMIN_EMAIL=harshbudhauliya882@gmail.com
INIT_ADMIN_PASSWORD=admin123
INIT_ADMIN_NAME=Harsh Budhauliya

# Database (optional)
# MONGODB_URI=mongodb://localhost:27017/convertflix

# URL used in password reset email link (defaults to localhost:5173 if not set)
# If your admin panel runs on a different URL, set it here
ADMIN_PANEL_URL=http://localhost:5173
# Legacy fallback if ADMIN_PANEL_URL is not set
# FRONTEND_URL=http://localhost:5173

# SMTP / Email (choose either SMTP_SERVICE or SMTP_HOST/PORT/SECURE)
# Option A: well-known service (e.g., gmail)
# SMTP_SERVICE=gmail
# EMAIL_USER=your-email@example.com
# EMAIL_PASS=your-email-app-password

# Option B: custom SMTP
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# EMAIL_USER=your-smtp-user@example.com
# EMAIL_PASS=your-smtp-password

# Optional: custom From header
# EMAIL_FROM=ConvertFlix <noreply@convertflix.com>

# Concurrency and performance tuning
# Maximum number of concurrent heavy jobs (image/video/audio/pdf processing)
# MAX_CONCURRENT_JOBS=2
# TOOLS_MAX_CONCURRENCY=2
# Sharp concurrency for image processing (set low to reduce memory)
# SHARP_CONCURRENCY=1
# FFmpeg threads (set to 1 to minimize memory/CPU spikes)
# FFMPEG_THREADS=1
# Max image pixels before auto-resize (e.g., 24_000_000 for ~24MP)
# MAX_IMAGE_PIXELS=24000000
# Max PDF size (MB) to attempt pdf-lib lightweight save before Ghostscript
# MAX_PDF_PDFLIB_MB=20
# Speed preset for processing: turbo | fast | balanced | quality
# TOOLS_SPEED_PRESET=fast
# SPEED_PRESET=fast

# Upload size limits (per type, in MB)
# MAX_IMAGE_SIZE_MB=50
# MAX_VIDEO_SIZE_MB=500
# MAX_AUDIO_SIZE_MB=200
# MAX_PDF_SIZE_MB=100
# MAX_ANY_SIZE_MB=100

# Express body parser limits (in MB)
# JSON_BODY_LIMIT_MB=50
# URLENCODED_BODY_LIMIT_MB=50
