# Frontend environment variables for ConvertFlix
# Copy this file to `.env` and adjust as needed.

# Base URL for the backend API. Must include the /api suffix.
# For local development with the backend on port 5000:
VITE_API_BASE_URL=http://localhost:5000/api

# Public site URL used to build absolute canonical and OpenGraph URLs in SEO.
# Example for production deployment:
# VITE_SITE_URL=https://flixconvert.taliyotechnologies.com
VITE_SITE_URL=http://localhost:5173
